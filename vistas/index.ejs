<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= typeof title !=='undefined' ? title : 'LuzShop' %>
    </title>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/estilo.css">
</head>

<body>

    <header>
        <div class="logo">LuzShop</div>
        <div class="search-bar">
            <input type="text" placeholder="Buscar tesoros de segunda mano...">
        </div>
        <button class="btn-upload" onclick="openModal()">+ Subir Ropa</button>
    </header>

    <main>
        <aside class="filters">
            <h3>Filtros</h3>
            <div class="filter-group">
                <label>Categoría</label>
                <select>
                    <option>Todas</option>
                    <option>Abrigos</option>
                    <option>Pantalones</option>
                    <option>Camisas</option>
                    <option>Calzado</option>
                </select>
            </div>
            <div class="filter-group">
                <label>Precio Máximo</label>
                <input type="range" min="0" max="200" step="10" style="width: 100%;">
            </div>
        </aside>

        <section class="product-grid">
            <% productos.forEach(producto=> { %>
                <div class="product-card">
                    <span class="tag">Second Hand</span>
                    <img src="<%= producto.image_url || 'https://images.unsplash.com/photo-1523381210434-271e8be1f52b?q=80&w=500' %>"
                        alt="<%= producto.nombre %>" class="product-image">
                    <div class="product-info">
                        <div class="product-category">
                            <%= producto.categoria %>
                        </div>
                        <div class="product-name">
                            <%= producto.nombre %>
                        </div>
                        <div class="product-price">$<%= Number(producto.precio).toFixed(2) %>
                        </div>
                    </div>
                </div>
                <% }) %>
        </section>
    </main>

    <div class="modal" id="uploadModal">
        <div class="modal-content">
            <h2 style="margin-bottom: 1.5rem;">Subir nueva prenda</h2>
            <form onsubmit="event.preventDefault(); closeModal(); alert('¡Ropa subida! (Simulación)');">
                <div class="form-group">
                    <label>Nombre de la prenda</label>
                    <input type="text" required placeholder="Ej: Chaqueta de cuero 90s">
                </div>
                <div class="form-group">
                    <label>Categoría</label>
                    <select>
                        <option>Abrigos</option>
                        <option>Pantalones</option>
                        <option>Camisas</option>
                        <option>Calzado</option>
                    </select>
                </div>
                <div class="form-group">
                    <label>Precio ($)</label>
                    <input type="number" step="0.01" required placeholder="0.00">
                </div>
                <div class="form-group">
                    <label>Imagen (URL de S3)</label>
                    <input type="url" placeholder="https://tu-bucket-s3.amazonaws.com/...">
                </div>
                <div style="display: flex; gap: 1rem; margin-top: 2rem;">
                    <button type="submit" class="btn-upload" style="flex: 1;">Guardar Prenda</button>
                    <button type="button" onclick="closeModal()" class="btn-upload"
                        style="flex: 1; background: #333;">Cancelar</button>
                </div>
            </form>
        </div>
    </div>

    <script>
        function openModal() {
            document.getElementById('uploadModal').style.display = 'flex';
        }
        function closeModal() {
            document.getElementById('uploadModal').style.display = 'none';
        }
    </script>

</body>

</html>